#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int num,j;
    int count=0;
    cin>>num;
    for(j=2;j<=num;j++){
        for(int i = 2;i<j;i++){
            if(j%i==0) 
            count++;
            
        }
        if(count==0) cout<<j<<" ";
        count=0;
        
            }
}
